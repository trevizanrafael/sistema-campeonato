<!DOCTYPE html>
<html lang="pt-BR" data-theme="<%= (typeof brand !== 'undefined' && brand.id) ? brand.id : 'base' %>">
  <head>
    <%- include("../partials/head", {
      titulo: titulo || "Nova equipe"
    }) %>
  </head>

  <body>
    <div class="app">
      <%- include("../partials/header") %>
      <%- include("../partials/sidebar") %>

      <main class="main-content">
        <%- include("../partials/messages") %>

        <div class="page-header">
          <div>
            <h1 class="page-title">Nova equipe</h1>
            <p class="page-subtitle">Cadastre uma equipe participante.</p>
          </div>
        </div>

        <section class="page-content">
          <div class="card form-card">
            <form action="/equipes" method="POST">
              <input
                type="hidden"
                name="_csrf"
                value="<%= csrfToken %>"
              >

              <%- include("_form", {
                equipe,
                erros
              }) %>

              <div class="form-actions">
                <a href="/equipes" class="button button-outline">
                  Cancelar
                </a>

                <button type="submit" class="button button-primary">
                  Cadastrar equipe
                </button>
              </div>
            </form>
          </div>
        </section>
      </main>

      <%- include("../partials/footer") %>
    </div>

    <%- include("../partials/scripts") %>
  </body>
</html>
