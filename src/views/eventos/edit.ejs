<!DOCTYPE html>
<html lang="pt-BR" data-theme="<%= (typeof brand !== 'undefined' && brand.id) ? brand.id : 'base' %>">
  <head>
    <%- include("../partials/head", {
      titulo: titulo || "Editar evento"
    }) %>
  </head>

  <body>
    <div class="app">
      <%- include("../partials/header") %>
      <%- include("../partials/sidebar") %>

      <main class="main-content">
        <%- include("../partials/messages") %>

        <div class="page-header">
          <div>
            <h1 class="page-title">Editar evento</h1>
            <p class="page-subtitle">Altere o nome ou a descricao.</p>
          </div>
        </div>

        <section class="page-content">
          <div class="card form-card">
            <form action="/eventos/<%= evento.id %>" method="POST">
              <input
                type="hidden"
                name="_csrf"
                value="<%= csrfToken %>"
              >

              <%- include("_form", {
                evento,
                erros
              }) %>

              <div class="form-actions">
                <a href="/eventos" class="button button-outline">
                  Cancelar
                </a>

                <button type="submit" class="button button-primary">
                  Salvar alteracoes
                </button>
              </div>
            </form>
          </div>
        </section>
      </main>

      <%- include("../partials/footer") %>
    </div>

    <%- include("../partials/scripts") %>
  </body>
</html>
